use Mix.Config

config :explorer, Explorer.Repo,
  database: "{{ ps_db }}",
  hostname: "{{ ps_host }}",
  username: "{{ ps_user }}",
  password: "{{ ps_password }}",
  pool_size: 20,
  timeout: 80_000
